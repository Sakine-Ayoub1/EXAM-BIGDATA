package sakine.ayoub;

public class Exercice1 {

   // 1. hadoop version

    // 2. start-dfs.sh
    //    jps

    // 3. hdfs dfs -mkdir -p /enset/bddc /enset/glsid

    // 4. touch java.txt
    //    touch cpp.txt

    // 5. hdfs dfs -put java.txt /enset/bddc/
    //    hdfs dfs -put cpp.txt /enset/glsid/

    // 6. hdfs dfs -ls /enset/bddc
    //    hdfs dfs -ls /enset/glsid

    // 7. hdfs dfs -cat /enset/bddc/java.txt

    // 8. hdfs dfs -du -h /enset/glsid/cpp.txt

    // 9. hdfs dfs -mv /enset/glsid/cpp.txt /enset/bddc/

    // 10. hdfs dfs -rm /enset/bddc/java.txt
    //     hdfs dfs -rm /enset/bddc/cpp.txt
}